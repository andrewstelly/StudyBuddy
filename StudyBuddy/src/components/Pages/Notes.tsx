import React from "react";
import TextDisplay from "../TextDisplay";

const Notes: React.FC = () => {
    return (
        <div>
            <TextDisplay title="Notes" content="This is the Notes page content." />
        </div>
    );
}

export default Notes;